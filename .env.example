# HTTP Basic Auth
AUTH_USER=root
AUTH_PASSWORD=root

# Server
PORT=3000
NODE_ENV=development

# Paths (optional overrides)
# STORAGE_DIR=./storage
# CONFIG_PATH=./config/settings.json

# Marketplace API (хранятся только в .env, не отображаются в UI)
OZON_CLIENT_ID=
OZON_API_KEY=
WB_API_TOKEN=
